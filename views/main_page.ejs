<% include ./partials/headerLandingLoginRegister %>

<header class="o-header">

              <article class="container">
                  <nav>

                      <!-- CURRENT USER -->
                      <% if(!currentUser){ %>
                        <div class="nav-links">   
                            <p class="currentUser"><a class="nav-text" href="/login">Login</a></p>
                           <p class="currentUser"><a class="nav-text" href="/register">Sign Up</a></p>
                           <p class="currentUser"><a class="nav-text" href="/about">About</a></p>
                        </div>
                           <% } else { %>
                            <div class="dropdown dropdownHoverEffect">
                                <p class="UserNameNot"><a class="nav-text" href="/user/<%= currentUser._id %>"> <%= currentUser.username %> </a></p>
                                <div class="dropdown-content">
                                    <p><a href="/user/<%= currentUser._id %>/info">User Informations</a></p>
                                    <p><a href="/user/<%= currentUser._id %>/add_pet">Add New Pet</a></p>
                                    <p><a href="/user/<%= currentUser._id %>/show_pet">View your pets</a></p>
                                    <p><a href="/user/<%= currentUser._id %>/reviews">Your Reviews</a></p>
                                    <p><a href="/logout">Logout</a></p>
                                </div>
                            </div>
                           <% }; %>
            
                        <div class="burger">
                            <div class="line1"></div>
                            <div class="line2"></div>
                            <div class="line3"></div>
                        </div>
                  </nav>
              </article>
          
          </header>

<main class="o-main">
    <!-- flashmessages goes here -->
    <section class="main-info">
        <h1>Pets<i class="fas fa-paw"></i>travel</h1>
        <% include ./partials/flashMessages %>
        <!-- SEARCH FORM!! -->
        <form class="formSearch" action="/search" method="GET">
            <label for="make_search">
                <input id="make_search" class="input" type="text" placeholder="Search for places like New York, Tokio, São Paulo, Mexico City, Johannesburg, Oslo, etc..." name="search">
            </label>
                <!-- <button>Submit</button> -->
        </form>
    </section>
              
    <% include ./partials/landingBackground %>

       </main>

<% include ./partials/footerLandingLoginRegister %>

